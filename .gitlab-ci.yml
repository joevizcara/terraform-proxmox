stages:
  - infra-change

change infra:
  stage: infra-change
  script:
    - tofu init
    - tofu version
    - tofu plan
#f
# rules:
#   - if: $CI_COMMIT_BRANCH == "master"
#     changes:
#       - init # any change in this file will trigger gitlab runner
