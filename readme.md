
# draft

```bash
bash <(curl -s https://gitlab.com/joevizcara/terraform-proxmox/-/raw/master/prep.sh)
```

> ![NOTE] Please examine the content of this shell script before executing it. You can test this on a Proxmox VE that is on a VM.
